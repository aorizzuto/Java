public class Challenge {
  public static boolean checkEnding(String str1, String str2) {
    int len2 = str2.length();
		int len1 = str1.length();
		
		String s = str1.substring(len1_len2, len1);
		
		return s.equals(str2);
  }
}
